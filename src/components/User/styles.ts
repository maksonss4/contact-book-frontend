import styled from "styled-components";

export const UserContainer = styled.div`
  padding: 10px 20px;
  width: 100%;
  max-width: 500px;

  h2 {
    color: var(--green);
    font-weight: bold;
  }

  p {
    color: var(--white);
  }
`;
